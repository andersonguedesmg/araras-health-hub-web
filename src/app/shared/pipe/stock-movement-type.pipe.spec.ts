import { StockMovementTypePipe } from './stock-movement-type.pipe';

describe('StockMovementTypePipe', () => {
  it('create an instance', () => {
    const pipe = new StockMovementTypePipe();
    expect(pipe).toBeTruthy();
  });
});
